# 赛博大脑依赖（克隆后 pip install -r requirements.txt 一次装齐）
# Python 3.10+
# --- 核心：向量检索 ---
fastembed>=0.3.0        # bge-small-zh-v1.5 向量模型（首次运行自动下载到 .model_cache/）
numpy>=1.24
# --- Web 界面（web_ui.py，可选）---
flask>=2.0              # 不用 Web 界面可注释掉
# --- MCP 服务器（mcp_server.py，可选）---
fastmcp>=4.0.0
# --- 文档解析（知识库导入 docx/pdf/pptx 时需要）---
python-docx>=1.1
PyPDF2>=3.0
python-pptx>=0.6
python-multipart>=0.0.9
