# AI 协作规约（赛博大脑）

> 这份文件给所有使用赛博大脑的 AI/Agent 看。目的是让会话之间的记忆能"接得上"。

## 一、铁律

1. **每日开工 / 长会话中段：必须先 recall**。
   - 命令：`python cyber_brain.py --db cyber_brain.db recall "<主题>"`
   - 不做这一步直接干活 = 凭训练数据瞎答，铁律/踩坑/客户台账全丢
2. **每个大任务告一段落：必须 session_log 打卡**。
   - 命令：`python session_log.py "一句话总结干了什么"`
   - 避免「上午做下午忘」——会话之间靠打卡续上
3. **每天开工：先跑 daily_brief**。
   - 命令：`python daily_brief.py`
   - 输出包含：滚动摘要 + 铁律 + 最近工作日志 + 客户台账 + 流水线指引
4. **重要事实 / 偏好 / 决策 / 铁律 / 踩坑：必须 add_fragment 入库**。
   - 长期记忆层（碎片），不是只写入 content_item
5. **删除 / 批量操作 / 跨工作区动作：必须先确认再执行**。
   - 遵循「防误删铁律」：任何删除/覆盖前先列清单、二次确认

## 二、session_log 打卡规约（2026-09-10 落地）

**目的**：解决「上午做完的事，下午/明天新会话已经忘了」的核心断点。

**触发时机**（满足任一即打卡）：
- 完成一个用户明确指定的任务（如"做知识库上传包 v2.1"）
- 完成一个可独立交付的子模块（如"实现 /api/today 接口"）
- 解决了一个具体问题（如"修复 web_ui.py syntax error"）
- 完成了调研/对比/选型（如"对比 8 个 MCP 记忆项目"）

**写法**：一句话总结（30-100 字），含动作动词 + 对象 + 关键产出
- ✅ "下午完成赛博大脑记忆机制优化：新建 session_log.py 会话自动打卡脚本"
- ✅ "提炼同赛道优秀作品借鉴清单：Hook 自动归档、compiled truth、时序溯源"
- ❌ "做了一些工作"（太模糊，不入）

**判重**：同一天相似度 > 75% 自动跳过，不重复打卡

**查看**：
- `session_log.py --today` 看今天
- `session_log.py --list 10` 看最近 10 条
- Web 端「今日」tab（`http://127.0.0.1:8899` → 今日）

## 三、来源标记规范

任何写入赛博大脑的内容必须带 `source_tag`：
- `session_log` — AI 自动打卡
- `manual` — 用户手动录入
- `web_upload` — Web 端录入
- `import_*` — 从某工作区/某文件导入
- `ai_synthesis` — LLM 合成/提炼的内容
- `external:<name>` — 来自外部系统（如飞书/TikHub）

## 四、不要做的事

- ❌ 凭训练数据回答"本公司有什么业务"——必须 recall / search 库
- ❌ 把临时调试结果写入 kb_document——用 `content --add ctype=note` 即可
- ❌ 删除 / 覆盖用户已有记忆——除非用户明确说"删"且有备份
- ❌ 推 git 时携带客户台账等敏感数据——公开仓库必须脱敏后再发布

## 五、当你看到这份文件时

如果你正打开一个新会话，先按 §1 步骤 1 + 3 跑一次，把"今天开工上下文"带进来再干活。
